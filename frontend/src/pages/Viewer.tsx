export default function Viewer() {
  return (
    <div>
      <h2>Interactive Viewer (stub)</h2>
      <p>
        This page will load the overlay JSON (<code>output/overlay/tracks.json</code>) and provide:
        filtering by amplitude/frequency, toggles to show regression overlays, and per-wave detail views.
      </p>
      <p>We’ll implement this after the pipeline events & partial artifacts are stable.</p>
    </div>
  );
}
